PKG_CXXFLAGS = $(SHLIB_OPENMP_CXXFLAGS)
PKG_LIBS = $(SHLIB_OPENMP_CXXFLAGS)
PKG_CXXFLAGS+=$(shell echo 'zlibbioc::pkgconfig("PKG_CXXFLAGS")'|\
    "${R_HOME}/bin/R" --vanilla --slave)
PKG_LIBS+=$(shell echo 'zlibbioc::pkgconfig("PKG_LIBS_static")' |\
    "${R_HOME}/bin/R" --vanilla --slave)
